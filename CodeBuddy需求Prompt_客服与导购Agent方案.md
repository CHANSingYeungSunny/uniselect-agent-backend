# 给 CodeBuddy 的任务 Prompt：客服 Agent 方案 + 导购 Agent 方案

> 使用方式：把下面整段（从"## 任务背景"到最后）复制粘贴给 CodeBuddy，作为一次性完整需求描述。
> 如果 CodeBuddy 支持上传文件，把 `选品Agent_PRD草案.md` 和 `Coze智能体准备配置指南.md` 两个文件一并上传，并在对话里说"请先阅读这两个附件再开始写方案"。

---

## 任务背景

我们是一家跨境电商相关业务团队，正在为"UniSelect 商家客服中心"设计两类新的 AI 智能体：

1. **客服 Agent**：接待消费者售前咨询、售后引导，敏感问题（退款/投诉/法律）转人工。
2. **导购 Agent**：面向消费者做商品推荐/搭配/种草式导购，帮助提升转化和客单价（区别于客服 Agent，导购 Agent 的核心目标是"促成购买决策"而不是"解决问题"）。

公司此前已经有一个类似产品的 PRD 范例——**选品 Agent PRD（TK 泰国市场 · 选品 + 品类战略）**，风格严谨、章节完整（背景/目标/用户故事/功能清单/流程/交互/数据指标/EARS 验收标准/边界场景/权限与数据口径/埋点/待确认问题/风险与排期/推荐规则细则/用户隔离与记忆分层）。**请完全模仿这份 PRD 的写作风格、章节结构、表格用法、EARS 验收标准写法**，为客服 Agent 和导购 Agent 分别撰写对应的 PRD 方案文档。

同时，我们已经有一份《Coze 智能体准备配置指南》，说明了如何在 Coze 平台上搭建客服类 Bot（人设 Prompt、商家业务 Prompt、知识库、发布 API、PAT Token、客服平台对接配置等）。**这份指南里的接入模式、字段命名（Bot ID / API Key / baseUrl / model / 商家 Prompt / 转人工规则）请在 Coze 版本方案中直接沿用和引用**，保持体系一致，不要另起一套命名。

---

## 需要产出的交付物（共 4 份主文档 + 1 份知识库专项方案）

### 1. 客服 Agent 方案（Coze 版本）
- 定位：基于 Coze 平台搭建，复用《Coze 智能体准备配置指南》里的接入流程与字段规范。
- 内容要求：
  - 按选品 Agent PRD 的章节结构撰写（背景/目标/用户故事/功能清单/流程/交互/数据指标/EARS 验收标准/边界场景/权限与数据口径/埋点/待确认问题/风险与排期）。
  - 明确写清楚：人设与系统规则怎么分层（平台维护 vs 商家可改的 Prompt）、知识库放什么内容、转人工规则怎么定义、和现有客服平台（agent.html / admin.html）字段如何对接。
  - 给出一张"Coze 侧配置项 ↔ 客服平台字段"的对照表（可直接复用配置指南第八章的表格结构）。

### 2. 客服 Agent 方案（代码直接实现版本）
- 定位：不依赖 Coze，自己用代码（LLM API + RAG + 业务规则引擎）实现同等能力。
- 内容要求：
  - 技术架构图（文字描述即可）：意图识别 → 知识库检索（RAG）→ Prompt 组装（系统规则层 + 商家业务层，对应 Coze 版本的两层分工原则）→ LLM 调用 → 敏感词/规则拦截 → 转人工判断 → 回复。
  - 说明使用什么向量数据库/检索方案、多轮会话怎么维护（sessionId 对应机制）、和 Coze 版本相比的优劣（部署成本、可控性、迭代速度、供应商锁定风险）。
  - 给出核心模块的接口设计（不需要真实代码，给函数签名/伪代码级别即可）。

### 3. 导购 Agent 方案（Coze 版本）
- 定位：基于 Coze 平台，但业务目标从"解决问题"转为"促成转化"。
- 内容要求：
  - 同样按选品 Agent PRD 结构撰写，但用户故事、功能清单要体现导购场景特有的能力，例如：
    - 基于消费者浏览/咨询意图做商品推荐与搭配；
    - 结合库存、活动、优惠券做话术引导；
    - 追问式挖掘需求（预算、场景、偏好）再推荐；
    - 推荐理由生成（可解释：为什么推这个）；
    - 转化路径埋点（曝光→点击→加购→下单）。
  - 说明导购 Agent 和客服 Agent 在人设/Prompt/知识库上可以共用的部分与必须区分的部分（避免重复建设）。

### 4. 导购 Agent 方案（代码直接实现版本）
- 内容要求同"客服 Agent 代码实现版本"的结构，但功能模块换成导购场景（推荐引擎、个性化排序、话术生成、转化追踪）。

### 5. 知识库搭建方案（单独一份，客服 Agent 和导购 Agent 共用）
这是本次最核心的新增需求，请重点展开：

**背景**：目前知识库是静态上传文档（txt/md/PDF/Excel），但商品信息（库存、价格、上下架状态、活动）频繁变化，需要**和公司商品系统/订单系统打通**，做成动态知识库，而不是每次人工改文档。

**请具体给出以下内容**：
- **数据分层方案**：哪些内容适合放"静态知识库"（商品规格、政策类，变化慢，走文档索引），哪些内容必须"动态查询"（库存、价格、订单状态、活动有效期，变化快，走实时 API/上下文注入），参考配置指南第 5.2 节的分工表思路，扩展成更完整的动态方案。
- **同步机制设计**：
  - 方案 A：定时同步（如每 N 分钟/小时把商品系统的数据导出、清洗、重新生成知识库文档并重新索引），适合变化不算特别快的信息（商品规格、政策）。
  - 方案 B：实时查询（Bot 侧通过工具调用/Function Calling，在对话中实时请求商品系统 API 获取库存、价格、订单状态），适合强时效信息。
  - 说明 Coze 版本怎么实现（Coze 的"插件/工具"能力对接商品系统 API）和代码直接实现版本怎么实现（RAG 检索 + Function Calling/Tool Use 结合）。
- **数据同步的技术方案**：
  - 谁触发同步（商品系统变更事件推送 vs 定时拉取）；
  - 数据清洗与格式化规则（原始商品数据库字段 → 知识库文档结构，可参考配置指南 5.4 节的 markdown 模板结构，设计一个自动生成脚本的思路）；
  - 索引更新策略（增量更新 vs 全量重建，索引生效延迟如何控制在可接受范围）；
  - 异常处理（商品系统接口不可用时如何降级，避免知识库显示脏数据或直接报错）。
- **权限与数据一致性**：多商家场景下如何保证每个商家的知识库/动态数据严格隔离（可参考选品 Agent PRD 里"用户隔离"章节的写法）。
- **验收标准（EARS 格式）**：知识库同步、降级、隔离相关的 Ubiquitous / Event-driven / Unwanted / State-driven / Optional 验收条款，风格对齐选品 Agent PRD 第八章、第十六章的写法。
- **待确认问题清单**：把还没定下来的技术选型（比如同步频率具体多少分钟、用什么消息队列/定时任务框架、是否需要专门的商品数据网关服务）列成表格，标注"待确认"。

---

## 写作风格与格式要求（务必遵守）

1. 完全对齐《选品 Agent PRD》的排版规范：
   - 一级标题用"一、二、三……"中文数字编号；
   - 大量使用表格呈现功能清单、对照表、指标、风险等；
   - 验收标准统一用 EARS 五分类（Ubiquitous / Event-driven / Unwanted / State-driven / Optional）；
   - 文档开头有版本号、状态、最后更新日期的元信息块。
2. 每份方案末尾都要有"待确认问题清单"表格和"风险与排期评估"章节，不要省略。
3. Coze 版本方案中，字段命名、流程步骤编号要和《Coze 智能体准备配置指南》保持一致，不要自创新术语。
4. 输出为 5 个独立的 Markdown 文件，文件名建议：
   - `客服Agent方案_Coze版.md`
   - `客服Agent方案_代码实现版.md`
   - `导购Agent方案_Coze版.md`
   - `导购Agent方案_代码实现版.md`
   - `动态知识库搭建方案.md`
5. 每份文档字数不要过短（参照选品 Agent PRD 的详尽程度），但也不要为了凑字数编造不合理的细节；技术选型上不确定的地方，明确标注"待确认/待评审"，不要假装已经定案。

---

## 补充说明

- 这两个 Agent（客服、导购）未来都会挂在同一个"UniSelect 商家客服中心"体系下，和已有的 Coze 接入流程共用同一套底层能力（工作空间、Bot、PAT、API 渠道），只是人设 Prompt 和知识库内容不同，请在方案里体现这种"底层复用、上层差异化"的关系。
- 如果需要对比 Coze 版本和代码直接实现版本的取舍，可以用一个简短的对比表格（维度：开发成本、上线速度、可定制性、供应商依赖、长期维护成本），放在两份方案共同的位置或单独一节说明。
