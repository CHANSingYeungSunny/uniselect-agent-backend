# 动态知识库搭建方案 · UniSelect 商家客服中心（客服 / 导购 Agent 共用）

> 版本：v0.1 草案
> 状态：待评审
> 最后更新：2026-08-15
> 关联文档：《Coze 智能体准备配置指南》v1.0、《客服 Agent 方案（Coze 版）》、《选品 Agent PRD 草案》v0.5

> 说明：本方案为客服 Agent 与导购 Agent **共用**的知识库专项方案，核心目标是从"静态上传文档"升级为"静态知识库 + 动态实时查询"混合架构，与公司商品系统 / 订单系统打通，避免每次人工改文档。两份 Agent 方案均引用本方案的"数据分层"与"字段级同步对照表"结论，不重复建设。

---

## 一、背景

### 1.1 业务背景
当前知识库为静态上传文档（txt / md / PDF / Excel），由商家人工维护。但商品信息中**库存、价格、上下架状态、活动有效期**变化频繁，人工改文档存在滞后与错漏，导致 AI 答复失真（如回复"有货"实际已售罄）。

UniSelect 已具备商品系统 / 订单系统数据源，本方案将其接入知识供给链路，构建**动态知识库**：变化慢的内容走文档索引（静态层），变化快的内容走实时 API 查询（动态层）。

### 1.2 用户痛点
| 用户类型 | 核心痛点 |
|------|------|
| 商家客服 | 商品改价/补货后需手动改知识库文档，易遗漏；活动过期信息仍被引用 |
| 平台管理员 | 静态库无法保证时效，AI 答旧值引发客诉与信任下降 |
| 消费者 | 收到"有货/某价"但实际不符，体验受损 |

### 1.3 现有方案缺口
- 静态文档无法感知商品系统实时状态；
- 无自动同步机制，依赖人工；
- 缺降级策略：商品系统不可用时，知识库可能显示脏数据或直接报错。

### 1.4 产品机会
构建"静态索引 + 动态查询"双轨知识供给：复用《Coze 智能体准备配置指南》5.2 分工表思路并扩展为字段级方案，Coze 版用"插件/工具"对接商品系统 API，代码直实现版用"RAG + Function Calling/Tool Use"结合，统一以 `merchant_id` 隔离。

---

## 二、目标

### 2.1 产品目标
建立与公司商品/订单系统打通的动态知识库，使 AI 答复中强时效字段（库存/价格/订单/活动）来自实时数据，稳数据来自索引文档，整体降低人工维护成本与答复失真率。

### 2.2 用户目标
- **商家客服**：商品改价/上架后无需手动改文档，系统自动同步或实时取数。
- **平台管理员**：统一管控同步机制与降级策略，保证多商家数据隔离与一致性。
- **消费者**：获得与商品系统一致的库存/价格/活动答复。

### 2.3 业务目标（方向性，待量化确认）
- 降低知识库人工维护工时（目标：快变字段零人工文档维护）；
- 提升强时效字段答复准确率（目标：库存/价格答复来自实时查询，非陈旧静态值）；
- 保证同步可用性（目标：商品系统不可用时降级不中断、不脏数据）。

---

## 三、用户故事

**故事 1（商家 · 改价免维护）**
> 作为商家，我改了商品售价，希望 AI 在消费者问价时直接给出新价，而不用我去改知识库文档。

**故事 2（商家 · 补货感知）**
> 作为商家，我补了库存，希望 AI 在消费者问"有货吗"时实时返回最新库存，而不是用旧文档说"无货"。

**故事 3（平台 · 隔离与一致）**
> 作为平台，我希望每个商家的知识库与动态数据严格以 `merchant_id` 隔离，A 店 Bot 绝不会读到 B 店库存。

**故事 4（消费者 · 降级体验）**
> 作为消费者，当商品系统临时不可查时，AI 应告诉我"库存暂不可查，建议联系人工"，而不是给我一个可能过时的数字或报错卡死。

**故事 5（导购 · 复用）**
> 作为导购 Agent，我希望复用同一套动态知识库（库存/价格/活动实时层 + 规格/政策静态层），只在不同推荐话术上做差异化。

---

## 四、功能清单

| 编号 | 功能 | 优先级 | 说明 |
|------|------|------|------|
| F1 | 数据分层（静态层 / 动态层） | P0 | 按字段变化频率划分存放位置，见第五章字段级对照表 |
| F2 | 静态层定时同步（方案 A） | P0 | 商品规格/图文/政策经导出-清洗-重建索引，建议 15-30 分钟或变更事件触发 |
| F3 | 动态层实时查询（方案 B） | P0 | 库存/价格/订单/物流经工具/Function Calling 实时取数 |
| F4 | 同步触发管理 | P0 | 变更事件推送（优先）或定时拉取（兜底） |
| F5 | 数据清洗与格式化 | P0 | 商品库原始字段 → 知识库 markdown 结构（参考指南 5.4 模板）自动生成 |
| F6 | 索引更新策略 | P0 | 增量更新 vs 全量重建；控制索引生效延迟 |
| F7 | 异常降级 | P0 | 商品系统不可用时降级，避免脏数据/整体报错 |
| F8 | merchant_id 隔离 | P0 | 知识库命名空间与动态查询商户参数行级隔离 |
| F9 | Coze 版工具对接 | P0 | Coze 插件/工具对接商品系统 API（实时层）+ 文档知识库（静态层） |
| F10 | 代码版 RAG + Tool Use | P1 | RAG 检索静态层 + Function Calling 调实时层 |
| F11 | 活动有效期校验 | P1 | 活动/优惠券走定时同步 + 有效期窗口校验 |

---

## 五、数据分层与字段级同步对照表（核心）

### 5.1 分层原则
- **静态知识库（文档索引）**：内容稳定、变化慢，适合检索——商品规格/图文/材质、售后政策、运费规则、高频 FAQ、活动说明。
- **动态实时查询（API/上下文注入）**：强时效、变化快，必须实时取数——库存、价格、订单状态、物流轨迹、活动有效期。

> 本表为客服 / 导购 Agent 共用的字段级锚点，EARS 验收标准（第八章）以本表字段为准。

### 5.2 字段级同步对照表
| 字段 | 同步方式 | 频率 / 触发 | 归属层 | 降级策略（锚点） |
|------|------|------|------|------|
| 库存数量 | 实时查询（Function Calling / 工具） | 每次对话触发该字段时查 | 动态层 | 失败 → 提示"库存暂不可查，建议联系人工"，不返回旧值 |
| 价格 | 实时查询 | 同上（每次对话触发时查） | 动态层 | 失败 → 提示"价格暂不可查"，不返回旧值 |
| 订单状态 | 实时查询 | 消费者提供订单号后查 | 动态层 | 失败 → 提示"订单状态暂不可查" |
| 物流轨迹 | 实时查询 | 发货后查询 | 动态层 | 失败 → 提示"物流暂不可查" |
| 商品规格 / 图文 | 定时同步重建知识库 | 建议 15-30 分钟 或 商品变更事件触发 | 静态层 | 索引延迟 → 答复标注"规格信息可能有延迟" |
| 售后政策 | 定时同步（低频） | 变更后手动触发 / 低频轮询（如每日） | 静态层 | 未就绪 → 转人工兜底 |
| 运费规则 | 定时同步 + Prompt 口径 | 变更后触发 | 静态层 | 同售后政策 |
| 活动 / 优惠券 | 定时同步 + 有效期校验 | 活动开始 / 结束时触发 | 静态层（带有效期） | 过期 → 不展示；失效 → 提示"活动已结束" |

> 注：具体同步频率（15 还是 30 分钟）、是否全量重建属待确认项（见第十二章 Q5）。

---

## 六、同步机制设计

### 6.1 方案 A：定时同步（静态层）
```
商品系统变更事件 / 定时触发器（FaaS，如每 15-30 分钟）
  → 拉取商品规格/政策等稳数据
  → 清洗 + 格式化为 markdown（参考指南 5.4 模板）
  → 生成/更新知识库文档
  → 重新索引（增量或全量）
  → 索引生效（控制延迟）
```
- 适用：商品规格、售后政策、运费、活动说明（变化慢）。
- 触发：优先"商品系统变更事件推送"；无事件总线时退化为"定时拉取"（兜底）。

### 6.2 方案 B：实时查询（动态层）
```
对话中命中库存/价格/订单/物流字段
  → Bot/LLM 调用工具（Coze 插件 或 Function Calling）
  → 请求商品系统 / 订单系统 API（带 merchant_id 参数）
  → 返回实时值 → 注入上下文 → 生成答复
  → 失败 → 降级提示（见 5.2 降级列）
```
- 适用：库存、价格、订单状态、物流轨迹、活动有效期校验。
- Coze 版：用 Coze"插件/工具"能力对接商品系统 API。
- 代码版：用 RAG 检索静态层 + LLM Tool Use / Function Calling 调实时层。

### 6.3 两种方案对比
| 维度 | 方案 A 定时同步 | 方案 B 实时查询 |
|------|------|------|
| 时效 | 分钟级延迟 | 秒级实时 |
| 成本 | 低（批量） | 中（每次查询） |
| 适用 | 稳数据 | 快数据 |
| 风险 | 索引延迟导致旧值 | 依赖 API 可用性（需降级） |

---

## 七、技术方案

### 7.1 谁触发同步
| 方式 | 说明 | 推荐 |
|------|------|------|
| 变更事件推送 | 商品系统发布变更事件（如 Kafka / RabbitMQ 消息）触发同步 | **推荐（实时性好）**，消息队列备选：Kafka / RabbitMQ / RocketMQ（待确认） |
| 定时拉取 | FaaS 定时任务（如云函数定时器 / Cron）周期性拉取 | 兜底，适合无事件总线的团队 |

### 7.2 数据清洗与格式化
- 原始商品库字段（sku、price、stock、spec、status、activity）→ 知识库 markdown 结构。
- 复用《Coze 智能体准备配置指南》5.4 模板（product_guide.md / policy.md / faq.md）结构，设计**自动生成脚本**思路：
  - 输入：商品系统导出（JSON / DB 视图）；
  - 处理：按 `merchant_id` 分桶 → 字段映射 → 渲染为 markdown（单段 200-500 字，便于检索）；
  - 输出：按商家隔离的知识库文档，送入索引。
- 清洗规则：删除无效 SKU、统一单位（价格币种、容量单位）、过滤个人联系方式。

> 说明：本方案仅给出"自动生成脚本"的概念级思路（输入/处理/输出三步）。具体的接口定义与伪代码级设计（函数签名 + 输入输出结构）见《客服 Agent 方案（代码实现版）》第七章 7.2 节，避免此处重复展开。

### 7.3 索引更新策略
| 策略 | 说明 | 适用 |
|------|------|------|
| 增量更新 | 仅重建变更商家/变更字段的文档与索引 | 变更事件触发时（推荐，延迟低） |
| 全量重建 | 周期性重建全部索引 | 兜底（如每日凌晨），防增量漂移 |

- 索引生效延迟控制：增量更新目标 < 1 分钟生效；全量重建在低频时段执行，避免影响在线检索。

### 7.4 异常处理与降级
- **商品系统 API 不可用**：实时查询失败 → 按 5.2 降级列提示，不返回旧值、不报错中断；会话仍可转人工。
- **索引未就绪**：静态层文档未索引完成 → 提示"知识库未就绪"，降级至实时层 + 转人工兜底。
- **脏数据防护**：同步失败的任务不覆盖线上索引（原子替换：新索引构建成功后才切换），避免半截数据生效。
- **活动过期**：带有效期的活动文档在过期后自动下架，不展示过期优惠。

### 7.5 Coze 版 vs 代码直实现版
| 维度 | Coze 版 | 代码直实现版 |
|------|------|------|
| 静态层 | Coze 知识库（上传/在线编辑文档） | 自托管向量库（Milvus / Redis 等，见备选） |
| 动态层 | Coze 插件/工具对接商品系统 API | LLM Tool Use / Function Calling |
| 检索 | Coze 内置检索 | 自研 RAG（召回 + 重排） |
| 隔离 | Bot 级 + 请求带 merchant_id | 索引命名空间 + 查询参数 merchant_id |
| 优劣 | 上线快、可控性弱、供应商锁定 | 可控性强、迭代自由、部署成本高 |

> 技术选型推荐（可替换备选）：向量库 **Milvus**（备选：Redis Stack / pgvector / Elasticsearch）；定时任务 **FaaS 定时器**（备选：K8s CronJob / Airflow）；消息队列 **Kafka**（备选：RabbitMQ / RocketMQ）。具体选型待确认（见第十二章）。

---

## 八、验收标准（EARS 原则，以字段为锚点）

### Ubiquitous（始终满足）
- 系统应始终以 `merchant_id` 行级隔离知识库与动态查询，任一商家不得读取他店数据。
- 系统应始终将库存、价格、订单状态、物流轨迹经实时查询取数，不得用静态文档中的旧值作答。
- 系统应始终在答复动态层字段时标注数据来源为实时查询（或降级提示）。
- 系统应始终在同步任务失败时保留线上旧索引，不写入半截/脏数据。

### Event-driven（事件触发）
- 当商品系统发布变更事件（库存/价格/规格）时，系统应触发对应层同步（动态层实时、静态层增量重建）。
- 当消费者提问命中库存/价格字段时，系统应通过工具实时查询商品系统并取数。
- 当活动到达结束时间时，系统应自动下架该活动文档，不再展示。

### Unwanted（异常/故障）
- **若库存字段实时查询返回失败**，系统应降级提示"库存暂不可查，建议联系人工"，不得返回陈旧静态值或报错中断。
- **若价格字段实时查询返回失败**，系统应降级提示"价格暂不可查"，不得返回旧值。
- 若商品系统 API 整体不可用，系统应使所有动态层字段统一降级提示，且会话仍可转人工，不得整体失败。
- 若静态层索引未就绪，系统应提示"知识库未就绪"并降级至实时层 + 转人工兜底。
- 若同步任务异常中断，系统应不切换线上索引，保留上一可用版本。

### State-driven（状态相关）
- 当字段处于"静态层（规格/政策）"状态时，系统应走文档索引检索，标注"信息可能有延迟"。
- 当字段处于"动态层（库存/价格）"状态时，系统应走实时查询，不得引用静态旧值。
- 当活动处于"已过期"状态时，系统应不展示该活动，不得引用过期优惠。

### Optional（可选）
- 若启用"变更事件推送"，系统应优先以事件触发同步而非定时拉取。
- 若启用"全量重建"，系统应在低频时段执行，不影响在线检索。

---

## 九、边界场景与异常状态

1. **库存超卖**：实时查询返回有货，但下单时已被占 → 属订单系统最终一致性，AI 答复以实时查询为准，下单冲突由订单系统处理。
2. **价格闪烁**：极短时间改价，AI 命中实时查询取最新值，无静态旧值风险（得益于动态层）。
3. **活动边界**：活动 23:59 结束，23:58 咨询仍展示，结束瞬间下架（有效期校验）。
4. **多商家串号**：请求漏带 `merchant_id` → 系统应拒绝查询并返回隔离错误，不得默认返回任一商家数据。
5. **索引漂移**：长期仅增量更新可能漂移 → 每日全量重建兜底（7.3）。
6. **降级风暴**：商品系统持续不可用 → 动态层统一降级提示，不重复报错。

---

## 十、权限与数据一致性（merchant_id 隔离）

- **隔离键**：以 `merchant_id` 为知识库命名空间与动态查询商户参数的行级隔离键（不沿用选品 PRD 的 `team_id` 概念，本体系以商家为最小隔离边界）。
- **静态层**：每个商家独立知识库空间（Coze 侧为独立 Bot 知识库 / 代码侧为独立索引命名空间），索引构建按 `merchant_id` 分桶。
- **动态层**：实时查询请求强制携带 `merchant_id`，商品系统按商户返回数据，越权请求拒绝。
- **一致性**：同一商家静态层与动态层数据应指向同一商品系统源，避免"静态说有货、动态说无货"矛盾；以动态层实时值为最终准（静态层仅作兜底与检索增强）。
- **越权处理**：任一请求访问非其所属商家数据时，系统应拒绝并返回无权限提示。

---

## 十一、埋点需求

| 埋点 | 用途 |
|------|------|
| 静态层同步触发 / 成功 / 失败 | 评估定时同步健康度 |
| 动态层实时查询触发 / 成功 / 失败 / 降级 | 评估实时层可用性与降级率 |
| 索引更新时延 | 评估生效延迟是否达标 |
| 活动上下架事件 | 评估有效期校验准确性 |
| merchant_id 隔离拒绝次数 | 评估越权尝试 |
| 脏数据/半截索引拦截 | 评估同步原子性 |
| 知识库命中（静态层） | 评估检索覆盖率 |

---

## 十二、待确认问题清单

> 已锁定：数据分层原则（静态层/动态层）；字段级同步对照表（第五章）；以 `merchant_id` 为隔离键；实时查询失败降级不返回旧值；Coze 版用插件/工具、代码版用 RAG+Tool Use。

| 编号 | 问题 | 状态 | 结论 |
|------|------|------|------|
| Q1 | 静态层定时同步频率具体多少分钟？ | 待确认 | 建议 15-30 分钟或变更事件触发，需与商品系统负载评估 |
| Q2 | 同步触发用消息队列还是定时拉取？ | 待确认 | 推荐 Kafka 事件推送 + FaaS 定时兜底；备选 RabbitMQ/RocketMQ |
| Q3 | 向量库选型（代码版）？ | 待确认 | 推荐 Milvus；备选 Redis Stack / pgvector / ES |
| Q4 | 是否需要独立商品数据网关服务？ | 待确认 | 建议增设网关统一封装商品/订单 API，降低 Bot 直连耦合 |
| Q5 | 增量 vs 全量重建策略与频率？ | 待确认 | 推荐变更事件增量 + 每日全量兜底 |
| Q6 | 实时查询超时阈值与重试？ | 待确认 | 建议 3s 超时、1 次重试后降级（与客服方案 Q3 对齐） |
| Q7 | 活动有效期校验精度（分钟级？） | 待确认 | 建议活动开始/结束事件触发下架，无需轮询 |
| Q8 | 导购 Agent 是否复用本动态知识库？ | 已确认（方向） | 是，共用静态层+动态层，仅话术差异化 |

---

## 十三、风险与排期评估

- **实时查询依赖风险（最高）**：强依赖商品系统/订单系统 API 可用性 → 已设计降级（不返回旧值、不报错、可转人工），但需商品系统保障 SLA；超时阈值待确认（Q6）。
- **索引延迟风险**：静态层定时同步有分钟级延迟 → 强时效字段已剥离到动态层，静态层仅放稳数据，影响面可控。
- **脏数据风险**：同步中断可能写半截数据 → 原子替换（新索引成功才切换）+ 失败保留旧版缓释。
- **隔离突破风险**：`merchant_id` 漏带可能串店 → 统一鉴权中间件 + 强制 merchant_id 参数作为安全底线。
- **供应商锁定（Coze 版）**：Coze 知识库与插件能力绑定 → 代码版提供可移植备选；两版共用本动态知识库逻辑，降低重复建设。
- **排期**：与《客服 Agent 方案（Coze 版）》同期评审；主要工作量在商品系统 API 对接、同步管道、降级策略。建议先落地动态层（实时查询）MVP，静态层定时同步并行推进。

---

## 附录：知识库 markdown 模板（复用指南 5.4，扩展 merchant_id 分桶）

**product_guide.md（按 merchant_id 分桶，自动生成）**
```markdown
# 商品资料（merchant_id: M-1001）

## 校园保温杯（编号 P-101，售价以实时查询为准）
- 容量：500ml；内胆：316 不锈钢；保温：12 小时
- 颜色：雾霾蓝/樱花粉/经典白
- 卖点：轻便 320g、防漏杯盖
- 库存：实时查询（不在此文档写死）
```

**policy.md**
```markdown
# 售后与物流政策（merchant_id: M-1001）
## 发货时效 / 运费 / 退换货（同 Coze 指南 5.4，按商家实际填写）
## 转人工场景：退款/投诉/法律 → 转人工
```

> 说明：价格与库存不在文档写死，统一由动态层实时查询；文档仅承载规格/政策等稳数据，降低过期风险。
