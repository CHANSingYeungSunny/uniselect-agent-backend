# 导购 Agent 方案（代码直接实现版）· UniSelect 商家客服中心

> 版本：v0.1 草案
> 状态：待评审
> 最后更新：2026-08-15
> 关联文档：《导购 Agent 方案（Coze 版）》、《客服 Agent 方案（代码实现版）》、《动态知识库搭建方案》、《Coze 智能体准备配置指南》v1.0、《选品 Agent PRD 草案》v0.5

> 说明：本方案为导购 Agent 的**代码直接实现版**。结构同《客服 Agent 方案（代码实现版）》，功能模块替换为导购场景（推荐引擎 / 个性化排序 / 话术生成 / 转化埋点）。系统规则层、转人工两级、merchant_id 隔离、动态知识库均复用既有文档，不重定义。

---

## 一、背景

### 1.1 业务背景
同《导购 Agent 方案（Coze 版）》第一章与《客服 Agent 方案（代码实现版）》1.1：自研代码实现导购 Agent，完全可控、无供应商锁定。业务目标为"促成转化"，与客服版"解决问题"区分。

### 1.2 与 Coze 版及客服代码版的关系
- **复用**：系统规则层硬编码不可覆盖机制（《客服 Agent 方案（代码实现版）》第八章）、转人工两级、merchant_id 隔离、动态知识库字段表。
- **差异**：模块从"问答/检索"换为"推荐引擎 / 个性化排序 / 转化埋点"；话术生成复用两层 Prompt 代码层实现。

---

## 二、目标
同《导购 Agent 方案（Coze 版）》第二章（转化导向）：推荐 CTR、加购率、下单转化率、客单价提升、合规红线不变。

---

## 三、用户故事
同《导购 Agent 方案（Coze 版）》第三章（意图推荐、追问挖掘、促单话术、敏感转人工、场景协同），本版差异仅在"上线方式=部署自研服务"，语义一致，不重述。

---

## 四、功能清单（导购模块）

| 编号 | 功能 | 优先级 | 说明 |
|------|------|------|------|
| F1 | 意图理解（导购向） | P0 | 提取预算/场景/偏好槽位 |
| F2 | 推荐引擎 | P0 | 结合实时库存/价格排序候选商品（静态层召回 + 动态层过滤） |
| F3 | 个性化排序 | P0 | 按匹配度/库存紧张度/活动力度/客单价贡献排序 |
| F4 | 话术生成（两层 Prompt） | P0 | 系统规则层硬编码 + 商家业务层 DB 拼接，生成推荐理由+促单 |
| F5 | 追问式挖掘 | P0 | 槽位不全时反问收敛 |
| F6 | Tool Use 动态查询 | P0 | 库存/价格/活动实时取（动态层） |
| F7 | 转化埋点采集 | P0 | 曝光→点击→加购→下单全链路 |
| F8 | 客服↔导购协同 | P1 | 同商家共用会话 + 上下文摘要注入 |
| F9 | 规则拦截 + 转人工 | P0 | 复用两级触发词，代码层强制 |
| F10 | merchant_id 隔离 | P0 | 检索/查询行级隔离 |

---

## 五、技术架构（文字描述）

```
消费者输入（sessionId + merchant_id + 槽位）
  → ① 意图理解（提取预算/场景/偏好）
  → ② 槽位不全？ → 追问挖掘（F5）回填
  → ③ 推荐引擎（F2）：RAG 静态层召回候选（命名空间=merchant_id）
  → ④ Tool Use 实时过滤（F6）：库存>0、价格在预算内、活动有效
  → ⑤ 个性化排序（F3）：匹配度 + 库存紧张度 + 活动力度 + 客单价贡献
  → ⑥ 两层 Prompt 组装（F4）：系统规则层硬编码 + 商家业务层 DB 拼接
  → ⑦ LLM 生成推荐理由 + 促单话术
  → ⑧ 规则拦截 + 转人工判断（两级触发词）
  → ⑨ 转化埋点（F7）：曝光
  → 输出
```

> 与客服代码版的区别：客服版是"检索→答"，导购版是"召回→实时过滤→排序→推荐生成"。系统规则层硬编码不可覆盖、merchant_id 隔离、动态层 Tool Use 机制完全一致。

---

## 六、交互说明
同《导购 Agent 方案（Coze 版）》第六章（目标差异、话术差异、三位一体回复结构、协同移交）。代码版差异仅在对接自研服务而非 Coze API。

---

## 七、数据指标
同《导购 Agent 方案（Coze 版）》第七章转化漏斗（曝光/CTR/加购率/下单转化率/客单价），本版补充：推荐引擎耗时 P95、排序失败降级率。

---

## 八、验收标准（EARS 原则）

### Ubiquitous（始终满足）
- 系统应始终以**服务端硬编码**系统规则层参与话术生成，商家业务层 DB 配置不得覆盖/绕过（复用《客服 Agent 方案（代码实现版）》第八章机制）。
- 系统应始终以 `merchant_id` 行级隔离（复用《动态知识库搭建方案》第十章）。
- 系统应始终保证每推荐品附可解释理由。
- 系统应始终在命中平台默认转人工词时转人工（复用两级机制）。

### Event-driven（事件触发）
- 当候选生成时，系统应经 Tool Use 实时过滤库存/价格/活动（动态层）。
- 当推荐曝光/点击/加购/下单发生时，系统应逐级记录转化埋点。
- 当意图由导购切售后时，系统应注入导购上下文摘要。

### Unwanted（异常/故障）
- **若推荐引擎无候选**（库存全空/预算无解），系统应降级提示"暂无可推荐"或转人工，不得编造商品。
- **若 Tool Use 超时/失败**，系统应降级提示"暂不可查"而非旧值（复用《动态知识库搭建方案》第八章）。
- **若两层 Prompt 组装失败**，系统应降级"仅系统规则层 + 转人工兜底"，不得让商家配置越权生效。
- 若个性化排序服务不可用，系统应降级为静态层相关度排序，不中断推荐。

### State-driven（状态相关）
- 当消费者已表达预算/场景时，系统应基于槽位推荐，不再追问。
- 当会话已转人工时，系统应停止导购生成。

### Optional（可选）
- 若启用搭配推荐，系统应基于实时库存生成搭配。
- 若启用多轮澄清，意图模糊时主动反问。

---

## 九、边界场景与异常状态
1. 库存全空 → 降级提示/转人工（同 Coze 版）。
2. 活动过期 → 促单话术不引用过期活动（动态层有效期校验）。
3. 排序服务故障 → 降级静态相关度排序。
4. 其余同《客服 Agent 方案（代码实现版）》第九章、《动态知识库搭建方案》第九章。

---

## 十、权限与数据口径
- 系统规则层硬编码、merchant_id 隔离、动态字段口径：**复用《客服 Agent 方案（代码实现版）》第十章 + 《动态知识库搭建方案》第十章**，不重定义。
- 导购业务层：商家 DB 配置聚焦推荐话术/搭配/优惠引导，范围与客服业务层一致（可改），不得触碰系统规则层。

---

## 十一、埋点需求（转化漏斗 + 技术）
同《导购 Agent 方案（Coze 版）》第十一章；本版补充：推荐引擎耗时、排序失败降级率、Prompt 组装失败率（与客服代码版对齐）。

---

## 十二、待确认问题清单

> 已锁定：复用客服代码版系统规则层硬编码机制、转人工两级、merchant_id 隔离、动态知识库字段表；推荐引擎/排序/转化埋点为导购特有。

| 编号 | 问题 | 状态 | 结论 |
|------|------|------|------|
| Q1 | 推荐引擎排序权重？ | 待确认 | 建议 匹配度 40% / 库存紧张度 20% / 活动力度 20% / 客单价贡献 20%（可调） |
| Q2 | 候选召回来源？ | 待确认 | 推荐自 RAG 静态层 + 商品系统全量（待定），实时过滤库存/价格 |
| Q3 | 转化漏斗基线？ | 待确认 | 同《导购 Agent 方案（Coze 版）》Q2 |
| Q4 | 向量库/LLM 选型？ | 待确认 | 同《客服 Agent 方案（代码实现版）》Q1/Q2（Milvus 推荐） |
| Q5 | 导购↔客服共用会话？ | 待确认 | 同《导购 Agent 方案（Coze 版）》Q1 |

---

## 十三、风险与排期评估

- **推荐相关性风险（最高）**：推荐不准伤转化 → 需实时库存/价格过滤 + 可解释理由 + 埋点回流优化排序权重（Q1 待定）。
- **过度促单风险**：系统规则层硬编码拦截越权承诺，与客服版同底线。
- **协同风险**：导购↔客服切换路由（Q5）需与客服代码版共用会话机制，避免重复建设。
- **双轨维护**：若 Coze 版与代码版并存，规则源须统一（同客服代码版 Q6）。
- **排期**：复用动态知识库与客服代码版基建；增量工作在推荐引擎、个性化排序、转化埋点。建议先落地"召回+实时过滤+基础排序"MVP，追问挖掘与搭配后续迭代。

---

## 附录 A：推荐引擎与排序接口级伪代码（承接《动态知识库搭建方案》7.2）

> 函数签名 + 输入输出结构（非可运行代码），呼应《动态知识库搭建方案》7.2 留白，并扩展导购特有模块。

```text
# 推荐请求（携带槽位）
struct RecRequest {
  merchant_id: string
  budget:      number        # 预算（可选）
  scene:       string        # 场景（可选）
  preference:  string[]      # 偏好标签（可选）
}

# 候选商品（实时层字段由 Tool Use 填充）
struct Candidate {
  sku:         string
  stock:       number        # 实时查询
  price:       number        # 实时查询
  match_score: number        # 静态层相关度
  activity:    object        # 活动（有效期校验）
}

# 推荐引擎签名
function recommend(req: RecRequest) -> Candidate[]:
  # 1. RAG 静态层召回（merchant_id 命名空间，按 scene/preference 相关度）
  # 2. Tool Use 实时过滤：stock>0、price<=budget、activity 有效
  # 3. 调用 personalize_rank 排序
  # 4. 返回 Top-N 候选

# 个性化排序签名
function personalize_rank(items: Candidate[], weights) -> Candidate[]:
  # score = match_score*w1 + stock_urgency*w2 + activity_strength*w3 + aov_contrib*w4
  # 默认权重待确认 Q1；返回降序 Top-N

# 转化埋点签名
function track_funnel(event: "impression"|"click"|"add_cart"|"order",
                      sku: string, session_id: string, merchant_id: string) -> void:
  # 写入转化漏斗表，供第七章指标计算
```

> 说明：stock / price / activity 来自动态层实时查询（详见《动态知识库搭建方案》第五章字段级对照表），不在静态文档写死。

---

## 附录 B：Coze 版 vs 代码实现版对比表（导购场景）

| 维度 | Coze 版（导购） | 代码直接实现版（导购） |
|------|------|------|
| 开发成本 | 低（配置为主） | 高（自建推荐引擎/排序/埋点） |
| 上线速度 | 快 | 慢 |
| 可定制性 | 弱（受 Coze 边界） | 强（排序权重/召回策略全可控） |
| 供应商依赖 | 强（锁定 Coze） | 无 |
| 长期维护成本 | 低（平台托管） | 中（自运维） |
| 推荐引擎 | Coze 插件 + Prompt 编排 | 自研 recommend + personalize_rank |
| 系统规则层机制 | Coze Bot 内锁定 | 服务端硬编码强制拦截 |
| 动态层实现 | Coze 工具 | LLM Tool Use |
| 转化埋点 | 平台/Coze 事件 | 自研 track_funnel |
| 适用商家 | 标准导购需求、快速上线 | 深度定制推荐策略、防锁定 |

> 对比结构复用《客服 Agent 方案（代码实现版）》附录 B，差异点调整为"导购场景下的推荐引擎/排序/转化埋点"。底层复用同一动态知识库（详见《动态知识库搭建方案》）。
