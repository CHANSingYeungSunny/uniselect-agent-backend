# Repo root: only the uniselect-cs-agent project is tracked.
# Ignore local-only working-set artifacts and editor/agent metadata.
.codebuddy/
.vscode/
*.iml
.idea/
oracleJdk-26/
_keep/
_git_push.ps1
tracked_now.txt

# Build output (handled inside uniselect-cs-agent/.gitignore too)
target/
bin/
build/
out/
