# 客服 Agent 方案（代码直接实现版）· UniSelect 商家客服中心

> 版本：v0.1 草案
> 状态：待评审
> 最后更新：2026-08-15
> 关联文档：《客服 Agent 方案（Coze 版）》、《动态知识库搭建方案》、《Coze 智能体准备配置指南》v1.0、《选品 Agent PRD 草案》v0.5

> 说明：本方案为客服 Agent 的**代码直接实现版**（不依赖 Coze，自研 LLM API + RAG + 业务规则引擎）。业务目标、用户故事、功能意图与《客服 Agent 方案（Coze 版）》一致，本章仅重写技术架构与实现机制。系统规则层与转人工两级机制、merchant_id 隔离、动态知识库字段表均复用既有文档，不重定义。

---

## 一、背景

### 1.1 业务背景
同《客服 Agent 方案（Coze 版）》第一章：UniSelect 商家客服中心需 AI 客服接待售前/售后。本版不使用 Coze 平台，而是以自有服务直接调用 LLM API，构建可完全自控的客服 Agent，避免供应商锁定、满足深度定制与私有化部署需求。

### 1.2 与 Coze 版的关系
- **共用**：业务目标、用户故事、功能意图、系统规则层语义、转人工两级机制、merchant_id 隔离、动态知识库字段表。
- **差异**：实现机制由"Coze Bot + 插件"变为"自研服务 + RAG + Tool Use"；系统规则层的"不可覆盖"由 Coze Bot 内锁定改为**服务端代码层强制拦截**。

---

## 二、目标

### 2.1 产品目标
以自研代码实现与 Coze 版等价的客服能力，获得对模型、检索、规则引擎、降级策略的完全可控性，支撑私有化与深度定制。

### 2.2 业务目标（基线待评审确认）
同《客服 Agent 方案（Coze 版）》2.3：AI 首字响应 P95 ≤ 2s、AI 闭环率 ≥ 60%、敏感问题转人工率 100%、实时查询占比 ≥ 99%、越权承诺 0。

---

## 三、用户故事
同《客服 Agent 方案（Coze 版）》第三章（平台管理员统一管控、商家快速上线、消费者售前咨询、敏感转人工、商家话术差异化），本版差异仅在"上线方式"由填 Coze 配置改为部署自研服务 + 配置商家业务层 DB，语义一致，不重述。

---

## 四、功能清单

| 编号 | 功能 | 优先级 | 说明 |
|------|------|------|------|
| F1 | 意图识别 | P0 | LLM + 结构化 schema 解析消费者输入为售前/售后意图 |
| F2 | RAG 静态层检索 | P0 | 向量检索知识库静态层（对接《动态知识库搭建方案》静态层） |
| F3 | 两层 Prompt 组装 | P0 | 系统规则层硬编码服务端不可变 + 商家业务层从 DB 读取拼接 |
| F4 | LLM 调用 | P0 | 调用自托管/第三方 LLM API 生成回复 |
| F5 | 规则拦截 | P0 | 服务端强制拦截越权表述（不承诺赔偿/改单等） |
| F6 | 转人工判断 | P0 | 复用两级触发词（平台默认 + 商家追加），命中即转 |
| F7 | Tool Use 动态查询 | P0 | 调商品系统 API 取库存/价格/订单/物流（动态层） |
| F8 | 多轮上下文 | P0 | sessionId 绑定 merchant_id 维护会话 |
| F9 | 降级策略 | P0 | Prompt 组装失败 / Tool Use 超时 → 降级提示或转人工 |
| F10 | merchant_id 隔离 | P0 | 检索命名空间 + 查询参数行级隔离 |
| F11 | 商家业务层管理 | P1 | 商家 Prompt / 追加转人工词存 DB，商家后台可改 |

---

## 五、技术架构（文字描述）

```
消费者输入（携带 sessionId + merchant_id）
  → ① 意图识别（LLM + schema）
  → ② RAG 检索静态层（向量库，命名空间=merchant_id；见《动态知识库搭建方案》）
  → ③ 两层 Prompt 组装
       系统规则层（硬编码，服务端不可变）
       商家业务层（从 DB 读取该 merchant_id 的配置拼接）
  → ④ LLM 调用（生成草稿）
  → ⑤ 规则拦截（服务端校验越权表述，命中即拦截/转人工）
  → ⑥ 转人工判断（两级触发词匹配）
  → ⑦ 动态数据：若涉库存/价格/订单/物流 → Tool Use 调商品系统 API（merchant_id 参数）
  → ⑧ 组装最终回复（静态召回 + 实时字段 + 规则约束）
  → 输出
```

> 关键差异：系统规则层不在 Coze Bot 内，而是**服务端硬编码常量**，商家业务层配置存于数据库；二者在组装时拼接，但商家配置的任何内容不得使系统规则层失效（代码层强制，详见第八章 EARS）。

---

## 六、交互说明
同《客服 Agent 方案（Coze 版）》第六章语义（入口形态、回复约束、转人工提示），差异仅在对接前端为自研客服服务而非 Coze API。一次回复 ≤3 要点、引用须真实等约束一致。

---

## 七、数据指标
同《客服 Agent 方案（Coze 版）》第七章指标表（首字响应 P95 ≤ 2s、AI 闭环率、敏感转人工率 100%、知识库命中率、实时查询成功率、越权承诺 0），本版额外关注：
- Prompt 组装失败率（降级触发占比）；
- Tool Use 超时率（降级触发占比）。

---

## 八、验收标准（EARS 原则）

### Ubiquitous（始终满足）
- 系统应始终以**服务端硬编码**的系统规则层参与 Prompt 组装，商家业务层 DB 配置**任何内容不得覆盖、降级或绕过**该层（呼应《客服 Agent 方案（Coze 版）》第十章不可覆盖约束，实现机制改为代码层强制拦截）。
- 系统应始终以 `merchant_id` 行级隔离 RAG 命名空间与 Tool Use 查询参数，任一商家不得读取他店数据（详见《动态知识库搭建方案》第十章）。
- 系统应始终在输出前完成规则拦截，越权表述一律不得放行。

### Event-driven（事件触发）
- 当消费者提问涉库存/价格/订单/物流时，系统应通过 Tool Use 调商品系统 API（带 merchant_id）实时取数。
- 当输入命中平台默认或商家追加转人工触发词时，系统应立即终止生成并转人工。

### Unwanted（异常/故障）
- **若两层 Prompt 组装失败**（如商家业务层 DB 不可读），系统应降级使用"仅系统规则层 + 转人工兜底"，不得因组装失败而让商家配置越权生效，也不得返回裸模型输出。
- **若 Tool Use 调用超时/失败**，系统应降级提示"该项暂不可查，建议联系人工"，不得返回陈旧值或报错中断（字段锚点详见《动态知识库搭建方案》第五章对照表）。
- 若 LLM API 无响应，系统应降级转人工，不得卡死。
- 若 RAG 静态层检索失败，系统应降级至仅实时层 + 转人工兜底。

### State-driven（状态相关）
- 当商家处于"未配置业务层"状态时，系统应仅以系统规则层 + 默认话术应答，不报错。
- 当会话处于"已转人工"状态时，系统应停止生成，仅人工接管。

### Optional（可选）
- 若商家启用"追加转人工关键词"，系统应在平台默认词之外追加匹配并转人工。
- 若启用"多轮澄清"，系统在意图模糊时主动反问收敛。

---

## 九、边界场景与异常状态
1. **商家业务层 DB 被篡改**：即使商家写入"可承诺退款"，服务端硬编码系统规则层仍拦截，不生效。
2. **Tool Use 返回脏数据**：以商品系统返回为准，失败走降级；不写缓存旧值。
3. **跨商家串号**：漏带 merchant_id → 拒绝查询并返回隔离错误。
4. 其余边界同《客服 Agent 方案（Coze 版）》第九章与《动态知识库搭建方案》第九章。

---

## 十、权限与数据口径
- 系统规则层硬编码：存放于服务端代码/配置中心，不可经商家后台修改（与 Coze 版"Bot 内锁定"语义等价，机制不同）。
- 商家业务层：存于数据库（按 merchant_id），商家后台可改，范围仅限卖点/口径/话术/追加转人工词。
- merchant_id 隔离：详见《动态知识库搭建方案》第十章。
- 动态字段口径：库存/价格/订单/物流须实时查询，见《动态知识库搭建方案》第五章。

### 10.1 统一规则配置源（双轨并存时的关键设计）
若 Coze 版与代码版长期并存（见第十二章 Q6），"系统规则层内容"与"转人工两级触发词"这类跨版本必须一致的逻辑，若各维护一份，会出现改一次要同步两边、易漂移的风险。本方案建议引入**统一规则配置源**机制：

| 项 | 说明 |
|------|------|
| 配置源 | 平台侧统一规则配置中心（一份权威源），承载：系统规则层文本、平台默认转人工触发词、merchant_id 隔离策略 |
| 下发到 Coze 版 | 平台默认规则经发布流程写入对应 Coze Bot 的"人设与回复逻辑"（仅平台可写，商家不可改），与《客服 Agent 方案（Coze 版）》第十章一致 |
| 下发到代码版 | 同一份规则源由配置中心下发到代码版服务端，作为硬编码常量加载（运行时不可经商家 DB 覆盖） |
| 商家可改部分 | 仅"商家业务层 / 追加转人工关键词"由各版本各自的商家后台维护（Coze 版 agent.html、代码版商家 DB），不进入统一规则源 |
| 一致性保障 | 规则源变更经平台发布流程统一生效；任一侧不得擅自修改系统规则层，改一处即两边同源，消除双轨漂移 |
| 备选 | 若暂不上配置中心，可用"单一 YAML/JSON 定义 + CI 构建时分别注入 Coze Bot 与代码服务"作为过渡，但需明确唯一编辑入口 |

> 说明：统一规则配置源解决的是"两份实现共享同一套不可变红线"的维护问题，不改变"系统规则层不可覆盖"的硬约束（Coze 版 Bot 内锁定、代码版服务端硬编码，机制不同但语义一致）。导购版（Coze/代码）复用同一规则源，不另起一份。

---

## 十一、埋点需求
同《客服 Agent 方案（Coze 版）》第十一章，本版补充：Prompt 组装失败次数、Tool Use 超时次数、降级触发分布。

---

## 十二、待确认问题清单

> 已锁定：复用《客服 Agent 方案（Coze 版）》两层分工语义与转人工两级机制、merchant_id 隔离、动态知识库字段表；系统规则层改为服务端硬编码强制拦截。

| 编号 | 问题 | 状态 | 结论 |
|------|------|------|------|
| Q1 | LLM 供应商选型？ | 待确认 | 推荐 豆包/Doubao 系列（中文优、成本低）；备选 DeepSeek / 通义千问 / 自建开源 |
| Q2 | 向量库选型？ | 待确认 | 推荐 Milvus；备选 Redis Stack / pgvector / Elasticsearch（同《动态知识库搭建方案》Q3） |
| Q3 | Tool Use 超时阈值？ | 待确认 | 建议 3s 超时、1 次重试后降级（与 Coze 版 Q3 对齐） |
| Q4 | 商家业务层存储？ | 待确认 | 推荐 关系库（MySQL/PostgreSQL）+ 缓存（Redis）；按 merchant_id 分表/行 |
| Q5 | 系统规则层如何热更新？ | 待确认 | 建议经统一规则配置源下发（见 10.1），商家无写权限；需与平台发布流程对齐 |
| Q6 | 与 Coze 版是否双轨并存？ | 待确认 | 建议按商家意愿二选一，底层复用同一动态知识库；若并存，须共用统一规则配置源（见 10.1）避免双轨维护漂移 |

---

## 十三、风险与排期评估

- **可控性收益（最高）**：代码版对模型/检索/规则/降级完全可控，无供应商锁定；代价是部署与运维成本高于 Coze 版。
- **系统规则层实现风险**：硬编码拦截须覆盖所有越权路径（输出前校验 + 组装约束），不能仅依赖 Prompt 措辞；建议单元测试覆盖越权样例。
- **Tool Use 依赖风险**：同《动态知识库搭建方案》，需商品系统 SLA 保障 + 降级。
- **双轨维护风险**：若 Coze 版与代码版并存，需保证两层分工/转人工/隔离逻辑同步演进，避免漂移。缓解机制见 10.1「统一规则配置源」：平台侧一份权威规则源，经发布流程分别下发到 Coze Bot（人设锁定）与代码版服务端（硬编码加载），商家仅可改业务层/追加转人工词，系统规则层任一侧不得擅自改；改一处即两边同源，消除双轨漂移（待确认 Q6）。
- **排期**：复用动态知识库（并行产出）；主要工作量在 RAG 管线、Tool Use 编排、规则拦截服务。

---

## 附录 A：数据清洗脚本接口级伪代码（承接《动态知识库搭建方案》7.2）

> 以下为函数签名 + 输入输出结构（非可运行代码），呼应《动态知识库搭建方案》7.2 节"具体接口/伪代码级设计见代码实现版"的留白。

```text
# 输入：商品系统导出的原始数据（按 merchant_id 分桶）
struct ProductRaw {
  merchant_id: string
  sku:         string
  price:       number        # 实时层字段，文档中不写死
  stock:       number        # 实时层字段，文档中不写死
  spec:        object        # 静态层：规格/图文/材质
  status:      string        # 上下架状态
  activity:    object        # 活动/优惠券（带有效期）
}

# 输出：按商家隔离的知识库 markdown 文档（仅静态层字段）
struct KnowledgeDoc {
  merchant_id: string
  doc_id:      string        # 如 "kb-{merchant_id}-product"
  markdown:    string        # 渲染后的 product_guide.md / policy.md
  segment_len: number        # 单段 200-500 字
}

# 处理函数签名
function build_knowledge_docs(raw: ProductRaw[]) -> KnowledgeDoc[]:
  # 1. 按 merchant_id 分桶
  # 2. 字段映射：spec/status/activity -> markdown（price/stock 不写入，留实时层）
  # 3. 清洗：删无效 SKU、统一单位、过滤联系方式
  # 4. 渲染为指南 5.4 模板结构
  # 5. 返回按商家隔离的文档列表（送入向量索引）

# 索引更新签名（增量/全量）
function upsert_index(docs: KnowledgeDoc[], mode: "increment"|"full") -> IndexResult:
  # mode=increment: 仅更新变更 merchant_id 命名空间
  # mode=full: 低频时段全量重建
  # 原子切换：新索引构建成功后才生效，失败保留旧版
```

> 说明：price / stock 在文档中留空、由动态层实时查询填充（详见《动态知识库搭建方案》第五章字段级对照表）。

---

## 附录 B：Coze 版 vs 代码实现版对比表（整体架构层面）

| 维度 | Coze 版 | 代码直接实现版 |
|------|------|------|
| 开发成本 | 低（配置为主，复用 Coze 能力） | 高（需自建 RAG/规则/降级服务） |
| 上线速度 | 快（八步配置即可上线） | 慢（需研发 + 部署 + 联调） |
| 可定制性 | 弱（受 Coze 能力边界限制） | 强（模型/检索/规则全可控） |
| 供应商依赖 | 强（锁定 Coze 平台） | 无（可换 LLM/向量库） |
| 长期维护成本 | 低（平台托管） | 中（自运维，但可控） |
| 系统规则层机制 | Coze Bot 内锁定（商家不可改） | 服务端硬编码强制拦截（代码层） |
| 动态层实现 | Coze 插件/工具 | LLM Tool Use / Function Calling |
| 适用商家 | 标准需求、快速上线 | 深度定制、私有化、防锁定 |

> 对比结构扩展自《动态知识库搭建方案》7.5 节，并上升到整体架构维度。两份方案底层复用同一动态知识库（详见《动态知识库搭建方案》）。
