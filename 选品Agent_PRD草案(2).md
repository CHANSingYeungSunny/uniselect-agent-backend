# 选品 Agent PRD 草案（TK 泰国市场 · 选品 + 品类战略）

> 版本：v0.5 草案（在 v0.4 基础上新增「第十六章 团队知识库与进度共享」，并同步更新功能清单/用户故事/验收标准/权限/埋点/风险/待确认问题）
> 状态：待评审
> 最后更新：2026-08-12

---

## 一、背景

### 1.1 业务背景
我们持有 **TikTok（TK）泰国市场**的数据资产：
- **产品元数据**：商品基础属性（类目、价格、形态等；**无成本/毛利数据**，仅有 price 与 units_sold，可派生销售额 = price × units_sold）；
- **半年度每周销量排名与销量数据**：排名为按**销量件数**排序、按**既定类目树**划分，仅在同类目内有可比性；
- 时间窗口为**近半年、周频次**，可观察趋势但非实时。

业务模式为**一件代发（dropshipping）**：无库存与预算约束，选品聚焦"卖什么 / 卖给谁 / 进哪类目"，不涉及备货资金与供应链自报。

店铺侧的历史表现与决策数据**可结构化接入（系统自动追踪）**（历史销量/排名/上架品类/采纳决策/上架动作）。

### 1.2 用户痛点
| 用户类型 | 核心痛点 |
|------|------|
| **新手卖家**（无店/刚起步） | 不知道**卖给谁（人群）**、**卖什么（产品）**、**进哪个类目**；缺乏市场判断力 |
| **有店卖家** | 不知道下一步该**深耕现类目**还是**探索新类目**；历史决策未沉淀，难做连续性判断 |

### 1.3 现有方案缺口
- 市面工具多为"给清单"，**不可解释**、**不结合店铺历史**、**不做战略层判断**；
- 新手缺乏"看懂市场"的引导，直接给品容易水土不服；
- 店铺决策与数据回流断裂，无法形成"建议→行动→再评估"的闭环。

### 1.4 产品机会
构建一个**用自然语言驱动、全程可解释**的选品 + 品类战略 Agent，同时服务"新手诊断"与"有店战略"两类场景，并以店铺历史决策回流形成持续闭环。

---

## 二、目标

### 2.1 产品目标
打造一个面向 TK 泰国市场的智能选品助手，将"买手/运营脑子里的选品逻辑"用对话转成可执行的数据筛选，并把决策过程摊开给人看。

### 2.2 用户目标
- **新手**：快速看懂市场机会，获得"切入品类 + 目标人群 + 入门选品"的可信建议；
- **有店卖家**：基于自身历史表现，获得"深耕 / 探索"的明确战略判断与具体选品清单。

### 2.3 业务目标（方向性，待量化确认）
- 提升选品**效率**（缩短从"想卖"到"定品"的决策时长）；
- 提升选品**质量/转化**（推荐被采纳且后续排名/动销向好）；
- **降低门槛**（让非专业卖家也能产出合理选品决策）；
- 保证**可解释**（每个推荐都能追溯数据与逻辑）。

---

## 三、用户故事

**故事 1（新手 · 市场机会）**
> 作为刚准备入场 TK 泰国的新手，我希望用一句话描述我的意向（如"想做高客单价、近期上升的女装"），Agent 能帮我扫描市场、反推目标人群，并给出建议切入的类目与入门选品清单及理由，让我敢下单。

**故事 2（有店 · 深耕/探索）**
> 作为已在 TK 泰国经营半年的卖家，我希望 Agent 结合我店铺的历史排名与决策，告诉我下一阶段该"深耕现类目"还是"探索新类目"，并给出对应选品清单，让我做对下一步。

**故事 3（有店 · 连续性）**
> 作为持续使用者，我希望我上次采纳的建议与后续上架动作被记录，下次 Agent 能基于"我真实的决策结果"而非凭空判断，让建议越来越准。

**故事 4（团队负责人 · 知识共享）**
> 作为带多位运营的卖家负责人，我希望团队把选品方法论、类目作战手册和踩坑经验沉淀到共享知识库，新人入职直接看、少踩坑，而不用从我脑子里一个个挖。

**故事 5（团队成员 · 进度互看）**
> 作为团队运营，我希望在团队看板上看到同事在深耕/探索哪些类目、最近采纳了什么、结果走势如何（脱敏摘要），便于分工、避免重复选品，也能借同伴的复盘少走弯路。

---

## 四、功能清单

| 编号 | 功能 | 优先级 | 说明 |
|------|------|------|------|
| F1 | 自然语言意图理解 | P0 | 将用户口语化描述解析为结构化选品条件（类目/价格带/趋势等），LLM + 结构化 schema 兜底防幻觉 |
| F2 | 条件确认（先确认后执行） | P0 | 解析结果回显给用户确认/修正，确认后才执行选品 |
| F3 | 市场数据检索 | P0 | 按类目树拉取产品元数据 + 周频销量排名趋势 |
| F4 | 店铺历史接入与诊断 | P0 | 接入店铺历史销量/排名/上架品类/决策，输出本店类目诊断 |
| F5 | 评分排序（评分卡） | P0 | 按"第十四章 推荐规则与数量"执行：硬门槛过滤 → 评分卡排序 → 多样性打散 → 战略覆盖 |
| F6 | 战略判断（深耕/探索） | P0 | 基于本店走势 + 类目机会（规则版，见补充规格 B6），输出明确战略判断与依据 |
| F7 | 理由生成（可解释） | P0 | 每品生成评分卡 + 人话总结 + 数据溯源 + 落选说明 |
| F8 | 受众反推（MVP 降级） | P1 | MVP 降级为"类目典型特征标签"（见补充规格 B3），非真实人群画像；辅助新手定位 |
| F9 | 结果呈现 | P0 | 候选清单 + 评分卡 + 理由，支持下钻/对比/导出 |
| F10 | 决策回流与模板 | P0 | **系统自动追踪**用户采纳与上架动作（一件代发场景，无需手动标注），记录为决策；支持把本次条件存为可复用模板 |
| F11 | 多轮澄清（可选） | P2 | 意图模糊时主动反问，收敛选品条件 |
| F12 | 用户隔离与实验记忆 | P0 | 各用户数据严格隔离鉴权；实验内记忆用户本轮全面选品，用于去重/全局视图/上下文连续 |
| F13 | 团队空间与成员管理 | P1 | 用户可创建/加入团队空间（组织内协作边界）；成员经邀请/审核加入；团队空间是显式、需同意的共享边界，与个体隔离正交 |
| F14 | 团队知识库 | P1 | 成员可沉淀/检索/协作文档（市场洞察、选品方法论、复盘、踩坑、类目作战手册、共享模板）；支持目录结构 + 搜索 |
| F15 | 团队进度共享 | P1 | 成员经开关向团队展示本人选品实验进度（当前战略 深耕/探索、已选类目、最近决策、结果走势），形成团队进度看板（脱敏摘要） |
| F16 | 团队级权限 | P1 | 知识库读写权限分级（查看/评论/编辑/管理）+ 团队管理员；跨团队严格隔离 |
| F17 | 知识沉淀回流 | P2 | 将本人决策记录(F10)/复盘经本人明确确认后一键沉淀为团队知识，降低重复建设 |

---

## 五、流程说明

### 5.1 新手路径
```
用户口语输入 → F1 解析为探索意图 → F2 确认条件
  → F3 市场扫描（类目机会）→ F8 受众反推
  → F5 评分排序 → F7 理由生成
  → 输出：建议切入类目 + 目标人群 + 入门选品清单（含理由）
```

### 5.2 有店路径
```
用户口语输入（含店铺上下文）→ F1 解析 → F2 确认条件
  → F3 市场数据 + F4 店铺历史
  → F4 本店类目诊断 + F6 深耕/探索判断
  → F5 评分排序 → F7 理由生成（关联店铺历史）
  → 输出：战略判断 + 选品清单 + 理由
  → F10 决策回流（用户采纳/上架 → 记录）
```

### 5.3 反馈闭环
系统**自动追踪**用户采纳建议与上架动作，回流为"决策记录"，成为下一轮 F4 店铺历史的输入，形成持续自驱闭环（一件代发模式下无需用户手动标注）。

---

## 六、交互说明

- **入口形态**：嵌入在**选品功能旁的对话窗口**（前端已存在），非独立页面；用户在进行选品操作时实时唤起。
- **确认环节（F2）**：解析出的结构化条件以卡片形式回显，用户可逐条修改或一键确认；未确认不执行选品。
- **结果页（F9）**：
  - 顶部：战略判断一句话（有店路径）或市场机会一句话（新手路径）；
  - 中部：候选清单，每品展开可见**评分卡 + 人话理由 + 数据溯源**；
  - 底部：导出（Excel/模板保存）、落选说明入口。
- **受众反推（F8）**：以"推断画像"卡片呈现（如"该类目买家偏年轻女性、价格敏感"），标注为推断非实测。

---

## 七、数据指标

| 指标 | 含义 | 说明 |
|------|------|------|
| 选品采纳率 | 用户确认/采纳推荐品的比例 | 衡量推荐质量 |
| 推荐品类后续排名变化 | 采纳建议后该类目/商品排名走势 | 衡量战略判断有效性 |
| 新手首轮决策时长 | 从首次输入到确认选品耗时 | 衡量降门槛/提效 |
| 理由可信度评分 | 用户对理由"看懂/可信"的反馈 | 衡量可解释 |
| 决策回流覆盖率 | 采纳建议后被记录为决策的比例 | 衡量闭环健康度 |

> 注：指标口径与基线需在评审中与数据同学对齐；部分指标（如战略判断准确率）需定义回溯评估方式。

---

## 八、验收标准（EARS 原则）

### Ubiquitous（始终满足）
- 系统应始终基于**既定类目树**进行排名对比，任何跨类目比较都必须显式标注类目归属。
- 系统应始终在输出每个推荐品时附带**可解释理由**（评分卡 + 溯源）。
- 系统应始终在执行选品前完成**条件确认（F2）**，未经确认不得返回选品结果。
- 系统应始终在**用户隔离**约束下访问数据，任一用户不得读取他人选品历史、决策或实验记忆。
- 系统应始终在一次实验内**记忆用户本轮已选清单**，并在后续推荐中据此去重。

### Event-driven（事件触发）
- 当用户提交自然语言选品意图时，系统应进行意图解析并回显结构化条件。
- 当用户确认结构化条件时，系统应触发市场数据与（有店时）店铺历史的检索与评分。
- 当店铺历史数据接入成功时，系统应生成本店类目诊断并参与战略判断。

### Unwanted（异常/故障）
- 若某数据源（市场/店铺）暂不可用时，系统应**降级**并明确提示缺失源，其余可用源继续出结果，不得整体失败。
- 若自然语言解析置信度低于阈值或意图歧义时，系统应**请求用户澄清**而非强行执行。
- 若商品周销量低于"趋势判断基数"阈值时，系统应**不对该品做趋势性判断**，并标注"数据不足"。
- 若类目树发生变更导致历史排名不可比时，系统应**提示口径变更**并限制跨期对比。

### State-driven（状态相关）
- 当用户处于"无店铺历史"状态时，系统应仅走新手路径，不展示深耕/探索判断。
- 当用户处于"已接入店铺历史"状态时，系统应在结果中关联并展示店铺历史上下文。
- 当用户处于"本次实验进行中"状态时，系统应在对话中保持实验内记忆（已选清单与上下文），并向用户展示本轮累计选品全局视图。

### Optional（可选）
- 若用户启用"模板保存"，系统应允许将本次结构化条件存为可复用模板。
- 若用户启用"多轮澄清"，系统在意图模糊时应主动反问收敛条件。

### 团队知识库相关（详见第十六章）
- **Ubiquitous**：系统应始终以 `team_id` 行级隔离团队共享数据，任一用户不得读取非其所属团队的知识或进度；成员未开启进度开关时，系统不得向团队暴露其任何进度摘要。
- **Event-driven**：当成员创建/编辑知识文档时，系统应在团队空间内更新并可被检索；当成员开启进度开关时，系统应将其脱敏进度摘要纳入团队进度看板。
- **Unwanted**：若某成员退出团队，系统应回收其团队内可见内容且不残留于他人视图；若进度摘要依赖的数据暂不可用，系统应降级提示"该成员进度暂不可见"，不影响其他成员看板。
- **State-driven**：当成员处于"未加入任何团队"状态时，系统不应展示任何团队知识库/进度入口；当成员为"团队管理员"时，系统应允许其管理成员与文档权限。
- **Optional**：若用户启用"知识沉淀回流"，系统应在本人明确确认后将复盘/决策沉淀为团队知识，未经确认不得自动公开个体数据。

---

## 九、边界场景与异常状态

1. **长尾低销品**：周销量低于阈值，排名噪声大 → 不纳入趋势判断，标注"数据不足"。
2. **类目树变更**：历史排名与新类目口径不一致 → 限制跨期对比并提示。
3. **外部/受众数据缺失**（原"四源"中的受众画像）→ 以反推代替，明确标注"推断"。
4. **数据滞后**：周频非实时，结果应标注"数据截至最近一周"。
5. **自动追踪失效**：若系统未能捕获某次上架动作，该轮闭环退化为单轮，不报错，下一轮仍可正常追踪。
6. **用户隔离越界**：任一用户请求访问非本人数据（历史/决策/实验记忆）时，系统应拒绝并返回无权限提示，不得泄漏他人选品信息。

---

## 十、权限与数据口径

- **数据权限**：市场数据（内部资产）与店铺数据（需店铺授权接入）应隔离鉴权。
- **用户隔离**：各用户的选品历史、决策记录、实验记忆须严格隔离；个体选品数据不跨用户暴露，仅经聚合/匿名化后作为"同市场其他参与者选品"竞争信号共享。
- **排名口径**：排名为按**销量件数**排序（已确认，非 GMV）；类目 = 既定类目树。同时持有**具体销量件数**，所有趋势/增长/集中度计算以具体销量为准，rank 仅用于类目内展示。
- **竞争信号**：暂无外部竞品数据通道；竞争以**同市场其他参与者的选品**（平台内可追踪、聚合匿名）作为补充信号，MVP 以市场排名（件数）为主要依据。
- **受众反推**：属推断结论，不得作为实测人群数据对外承诺。

### 团队共享边界（新增，详见第十六章）
- **默认私有、显式同意才共享**：未加入团队或未开启进度开关时，个体行为与 v0.4 完全一致（严格隔离）；仅成员**主动贡献的知识文档**与**开启开关后暴露的脱敏进度摘要**进入团队可见域。
- **进度脱敏**：团队进度看板只展示汇总/类目级指标（当前战略、类目分布、最近决策汇总、排名走势），不展示他人具体选品明细与店铺敏感数据，避免"共享进度"变相突破个体隔离。
- **跨团队隔离（硬约束）**：知识库严格限定在所属团队（team_id）内，不同团队空间之间数据行级隔离，类比个体隔离；**知识库不可跨团队共享**（已确认 Q10）。
- **与竞争信号并存**：第十章"同市场其他参与者选品"为跨用户匿名聚合（不限于同团队）；团队知识库为同团队内命名共享，两者并存、互不替代。

---

## 十一、埋点需求

| 埋点 | 用途 |
|------|------|
| 意图输入文本 + 解析结果 | 评估解析准确率、优化 schema |
| 条件确认/修改行为 | 评估澄清频率 |
| 推荐品曝光/展开/采纳 | 计算采纳率与理由可信度 |
| 战略判断展示与用户后续动作 | 回溯战略判断有效性 |
| 决策回流记录 | 闭环覆盖率 |
| 团队空间创建/加入/退出 | 评估协作渗透 |
| 知识文档创建/编辑/检索/点赞 | 评估知识库活性 |
| 进度开关开启/关闭 | 评估共享意愿 |
| 团队看板访问频次 | 评估进度共享使用 |
| 知识沉淀回流动作 | 评估复用价值 |

---

## 十二、待确认问题清单

> 已锁定：推荐数量（默认 5、用户可指定）、推荐规则（四阶段全规则）详见第十四章；Q1–Q7 均已确认；**Q8/Q10/Q11 团队相关决策已于 v0.5 确认（团队由用户显式定义成员与角色、知识库不可跨团队、团队功能纳入首期）；Q9 进度默认开关仍待确认**。

| 编号 | 问题 | 状态 | 结论 |
|------|------|------|------|
| Q1 | 排名口径"销量"= 件数/GMV？ | 已确认 | **件数**（非 GMV） |
| Q2 | Agent 载体？ | 已确认 | 嵌入选品旁的对话窗口（前端已存在） |
| Q3 | 决策记录来源？ | 已确认 | **系统自动追踪**（一件代发，无需手动标注） |
| Q4 | 新手是否自报预算/供应链？ | 已确认 | 一件代发，**不考虑预算/供应链自报** |
| Q5 | 评分维度权重？ | 已确认 | 系统默认（匹配30/趋势25/变现力20/竞争15/受众10）+ 可调 |
| Q6 | 竞品/外部数据通道？ | 已确认 | 暂无外部通道；竞争=同市场其他参与者选品；MVP 核心源先上 |
| Q7 | MVP 范围？ | 已确认 | **核心源先上**（市场数据 + 店铺历史），增强源可降级 |
| Q8 | 团队的定义与边界？ | 已确认 | **由用户（管理员）显式定义团队成员与角色**；角色体系先预留扩展能力（即使当前不确定具体划分也保留该能力），不依赖系统自动判定 |
| Q9 | 进度共享默认开关？ | 待确认 | 建议**默认关闭**（opt-in），成员主动开启才展示；需确认是否强制负责人可见团队进度 |
| Q10 | 知识库是否可跨团队？ | 已确认 | **不可跨团队**：知识库严格限定在所属 team_id 内，为硬约束，任何组织内多团队亦不可公开互通 |
| Q11 | 团队功能是否纳入首期 MVP？ | 已确认 | **纳入首期**：F13–F16 进首期交付，与选品核心同期排期但可独立灰度 |

---

## 十三、风险与排期评估

- **排期风险（已缓释）**：采用"核心源先上"（市场数据 + 店铺历史），竞品外部/受众增强源可降级，MVP 联调面收窄。
- **数据口径风险（已对齐）**：销量=件数、类目=既定类目树已确认；剩余待对齐项为"趋势判断基数阈值"与"类目树变更机制"。
- **解析幻觉风险**：自然语言→结构化条件须有 schema 兜底 + 确认环节，否则理由不可信。
- **闭环风险（已缓释）**：决策记录改为系统自动追踪，闭环可自驱，不再依赖手动标注。
- **P0 阻塞（已解决）**：研发评审报告的 7 个 P0 阻塞（数据字典 / 评分公式 / 受众 / 实验生命周期 / 同参与者 / 战略规则 / 跨类目度量）已由《选品 Agent 技术规格补充》给出可落地规格，待数据/算法确认字段名与阈值后解除阻塞。
- **团队功能风险（已纳入首期）**：团队功能已确认纳入首期 MVP（F13–F16），需与选品核心同期排期但可独立灰度；知识库价值依赖内容密度，存在冷启动/活性风险（建议负责人带头沉淀方法论+复盘、以新人 onboarding 为首批强制场景）；同时团队进度共享须以**脱敏摘要**为硬边界，严禁直接读取他人原始选品/店铺数据，需统一鉴权中间件 + `team_id` 行级隔离作为安全底线；**知识库不可跨团队（已确认 Q10，硬约束）**。

---

## 十四、推荐规则与数量明细

> 评分维度量化公式、归一化方法、MVP 降级说明见《选品 Agent 技术规格补充》（B2 / B5）。

### 14.1 每次推荐数量
- **默认 5 个**（Top 5 by 总分）；
- **用户可在自然语言输入或确认环节自行指定数量**（如"给我 8 个""只要 3 个"），系统以用户指定为准；
- 未指定时回落到默认 5 个。

### 14.2 推荐规则（四阶段）

**阶段一 · 硬门槛过滤（不满足直接不进候选）**
| 门槛 | 规则 | 说明 |
|------|------|------|
| 销量基数 | 周销量 ≥ 阈值才参与趋势评判 | 否则标注"数据不足"，不评趋势性 |
| 销售额/客单价 | ≥ 用户意图阈值或系统默认下限（基于 price×units_sold 派生，非真实毛利） | 与"高客单价/高销售额"类意图强相关 |
| 类目匹配 | 命中意图类目 / 战略指定类目 | 跨类目不混比（类目树约束） |
| 可操作性（一件代发放宽） | 一件代发下供应链可得性非瓶颈，不再作为硬门槛；仅排除明显违规/不可上架品 | 原为"非垄断不可得"，已按一件代发场景放宽 |

**阶段二 · 评分卡打分（各维度 0–100，加权求和）**
- 维度：匹配度(w1) · 变现力(w2，销售额规模=价×量) · 趋势性(w3，近 N 周销量增长率) · 竞争度(w4，类目销量集中度越分散越优；**MVP 暂不含同参与者项**，见补充规格 B5) · 受众契合(w5，类目典型特征标签匹配)
- 权重（系统默认，用户可在确认环节微调）：匹配度 **30%** · 趋势性 **25%** · 变现力 **20%** · 竞争度 **15%** · 受众契合 **10%**（合计 100%）。默认偏向"贴合意图 + 抓上升势头"，用户可按场景上调变现力或竞争度权重。

**阶段三 · 排序 + 多样性打散**
- 按总分降序；
- 同类目 / 子类目最多占 K 个，防止推荐高度雷同品；
- 取 Top N（默认 5，用户可指定）。

**阶段四 · 战略覆盖（仅有店路径）**
- **深耕**：候选主要来自现类目内高匹配品；
- **探索**：单独列出 Top M 个跨类目高机会品，与深耕结果分区展示。

### 14.3 落选说明规则
- 对得分高但被硬门槛/多样性/战略分区排除的品，应在结果页"落选说明"中标注原因（如"销售额/客单价低于阈值""同类目已占满 K 个""属探索区未进深耕主列表"），保证可解释闭环。

---

## 十五、用户隔离与实验内记忆

> 实验生命周期（起止边界、idle、持久化）的详细定义见《选品 Agent 技术规格补充》（B4）。

### 15.1 用户隔离
- 各用户的选品历史、决策记录、实验记忆**严格隔离鉴权**，任一用户不可访问他人数据。
- 个体选品数据**不跨用户暴露**；仅经聚合/匿名化后作为"同市场其他参与者选品"竞争信号共享（见第十章）。
- 系统应始终以当前操作用户身份校验数据访问范围。

### 15.2 实验内记忆（本次实验）
- 在一次"实验/会话"内，Agent 应**持续记忆用户本轮全面选了什么**（已选清单），包括跨多次推荐、多次采纳的累计结果。
- 记忆用于：
  - **去重**：不重复推荐用户已选/已采纳的品；
  - **全局视图**：向用户展示"本次实验已选 N 个品，覆盖类目 A/B/C"；
  - **上下文连续**：多轮对话中保持选品上下文一致（深耕/探索不矛盾）。
- 实验记忆与跨轮决策历史（F4/F10）的区别：前者为**本次实验内**的临时累计视图，后者为**跨实验**持久化的自动追踪记录。

### 15.3 两层记忆关系
| 层 | 范围 | 持久性 | 用途 |
|------|------|------|------|
| L1 实验内记忆 | 单次实验/会话 | 实验内（可保存为实验记录） | 去重、全局视图、上下文连续 |
| L2 跨轮决策历史 | 跨实验（同一用户） | 持久 | 深耕/探索战略判断（F4/F10） |

> 两层记忆均运行在**用户隔离**之上：L1 仅属当前用户本次实验，L2 仅属当前用户跨实验，互不越权。

---

## 十六、团队知识库与进度共享（新增）

> 本章为 v0.5 新增。核心思路：**不推翻 v0.4 的严格用户隔离，而是在其上引入一层"显式同意的共享边界"（团队空间 + 进度脱敏开关）**。

### 16.1 背景与动机
- 现有 PRD（v0.4）以**个体用户**为核心，强调严格用户隔离（F12、第十章、第十五章），个体选品数据不跨用户暴露，仅经聚合/匿名化为"同市场其他参与者选品"竞争信号（第十章）。
- 但实际使用中，**一个组织（品牌方/卖家公司）往往有多位运营/买手**共同经营 TK 泰国市场；他们彼此需要共享"选品方法论、踩坑、复盘"等**知识**，并了解同事"在深耕/探索哪些类目、最近采纳了什么"等**进度**，避免重复选品、加速新人成长。
- 团队知识库不是推翻隔离，而是引入一个**显式同意的、命名化的共享边界（团队空间）**——它是现有"匿名聚合竞争信号"之上、更结构化、更可控的协作层。

### 16.2 设计原则（与用户隔离不冲突）
- **团队是显式共享边界**：由用户（管理员）**显式定义**哪些用户同属一个团队、并为其指派角色；未加入团队时，行为与 v0.4 完全一致（严格隔离）。角色体系即使当前划分未定，也**保留扩展能力**。
- **原始个体数据默认不共享**：成员的市场数据、店铺历史、L1 实验内记忆、L2 跨轮决策历史**默认私有**；仅以下两类内容进入团队可见域：
  1. 成员**主动贡献**的知识文档（F14）；
  2. 成员**开启进度开关**后暴露的、经脱敏的进度摘要（F15，不含原始选品明细与店铺敏感数据）。
- **三层记忆关系补充**（在第十五章 L1/L2 之上增加团队层）：

  | 层 | 范围 | 持久性 | 可见性 |
  |----|------|--------|--------|
  | L1 实验内记忆 | 单次实验/会话 | 实验内 | 仅本人 |
  | L2 跨轮决策历史 | 跨实验（同用户） | 持久 | 仅本人（除非主动沉淀为团队知识 F17） |
  | L3 团队共享层 | 团队空间 | 持久 | 团队成员（经成员同意/贡献） |

- **跨团队严格隔离**：不同团队空间之间数据不可见，类比个体隔离；团队空间内亦遵循最小可见原则。

### 16.3 目标用户与场景
| 角色 | 场景 |
|------|------|
| 团队负责人/主理人 | 把选品方法论、类目作战手册、踩坑沉淀到共享知识库，让新人少踩坑、快速上手 |
| 运营/买手（成员） | 在团队看板看到同事深耕/探索的类目与最近决策，便于分工、避免重复选品 |
| 新人 | 入职先看团队知识库（方法论+复盘），而非逐个问老人 |

### 16.4 功能详述
- **F13 团队空间**：由用户（管理员）**显式定义**团队成员与角色（邀请/审核/移除）；即使当前不确定具体角色划分，也**保留角色体系扩展能力**；团队 ID 作为共享数据隔离键。
- **F14 团队知识库**：文档创建/编辑/协同（富文本，@成员，评论）、目录结构、搜索（标题+正文）；内容类型含市场洞察、选品方法论、决策复盘、踩坑记录、类目作战手册、共享模板（复用 F10 模板能力，扩展为团队级）。
- **F15 团队进度共享**：成员开关控制是否向团队展示进度；展示内容为脱敏摘要——当前战略（深耕/探索）、已选类目分布、最近 N 条决策（采纳/上架）、采纳后排名走势（仅汇总指标，不含个体选品明细）。
- **F16 团队级权限**：基于用户定义的角色映射为权限（查看 / 评论 / 编辑 / 管理 四级，角色体系可扩展）；管理员可管理成员、角色与文档权限；**知识库严格不跨团队**。
- **F17 知识沉淀回流**（P2）：将本人决策记录（F10）/复盘经本人明确确认后一键沉淀为团队知识，降低重复建设；沉淀动作需本人确认，不自动公开个体数据。

### 16.5 流程说明
```
成员加入团队空间（F13，邀请/审核）
  → 贡献知识：撰写/编辑知识文档（F14）→ 团队成员检索/协作文档
  → 共享进度：开启开关（F15）→ 团队进度看板聚合成员脱敏摘要
  → （P2）沉淀回流：将本人复盘/决策经确认沉淀为团队知识（F17）
```
> 团队看板的"进度"数据来自各成员**已授权的脱敏摘要**，不回溯读取他人原始选品/店铺数据；与 F4/F10 自动追踪链路解耦——进度共享是"视图聚合"，不替代个体决策记录。

### 16.6 数据模型要点（草案）
- `team_space`（团队空间：id、名称、管理员、创建者）
- `team_member`（成员关系：user_id、team_id、角色、进度共享开关）
- `kb_doc`（知识文档：id、team_id、作者、类型、内容、权限、版本）
- `team_progress_snapshot`（进度摘要：user_id、team_id、战略、类目分布、最近决策汇总、排名走势指标）——由成员端按开关生成，只读聚合。

### 16.7 权限模型（团队共享边界）
- **个体隔离不变**：未加入团队 / 未开启开关时，行为与 v0.4 完全一致。
- **团队内**：按 F16 四级权限；知识文档遵循"创建者/管理员可控可见性"。
- **不可跨团队（硬约束，已确认 Q10）**：知识库严格限定在所属 `team_id` 内，不同团队数据行级隔离，任一用户不得跨团队读取或检索。
- **越权处理**：任一请求访问非本人且非其所属团队的数据时，系统应拒绝并返回无权限提示。

### 16.8 验收标准（EARS，团队相关）
- **Ubiquitous**：系统应始终将团队共享数据以 `team_id` 行级隔离，任一用户不得读取非其所属团队的知识或进度；系统应始终在成员未开启进度开关时，不向团队暴露该成员任何进度摘要。
- **Event-driven**：当成员创建/编辑知识文档时，系统应在团队空间内更新该文档并可被成员检索；当成员开启进度共享开关时，系统应将其脱敏进度摘要纳入团队进度看板。
- **Unwanted**：若某成员退出团队，系统应将其团队内可见内容按权限回收，且不残留于其他成员视图；若进度摘要生成所依赖的成员数据暂不可用，系统应降级提示"该成员进度暂不可见"，不影响其他成员看板。
- **State-driven**：当成员处于"未加入任何团队"状态时，系统不应展示任何团队知识库/进度入口；当成员处于"团队管理员"状态时，系统应允许其管理成员与文档权限。
- **Optional**：若用户启用"知识沉淀回流"，系统应在本人明确确认后将复盘/决策沉淀为团队知识；未经确认不得自动公开个体数据。

### 16.9 边界与异常
1. **进度脱敏**：团队进度看板只展示汇总/类目级指标，不展示他人具体选品明细、店铺敏感数据；避免"共享进度"变相突破个体隔离。
2. **退出团队**：成员退出后，其贡献的知识文档归属团队（除非含仅本人可见标记）；其进度摘要立即从看板移除。
3. **知识质量**：团队知识库价值靠内容密度，需冷启动策略（负责人带头沉淀方法论/复盘）。
4. **与竞争信号的区分**：第十章"同市场其他参与者选品"为跨用户匿名聚合（不限于同团队）；团队知识库为同团队内命名共享。两者并存、互不替代。

### 16.10 埋点（团队相关）
| 埋点 | 用途 |
|------|------|
| 团队空间创建/加入/退出 | 评估协作渗透 |
| 知识文档创建/编辑/检索/点赞 | 评估知识库活性 |
| 进度开关开启/关闭 | 评估共享意愿 |
| 团队看板访问频次 | 评估进度共享使用 |
| 知识沉淀回流动作 | 评估复用价值 |

### 16.11 风险
- **冷启动/活性（最高）**：知识库价值依赖内容密度，建议负责人带头沉淀"方法论+复盘+踩坑"，并以新人 onboarding 为首批强制场景。
- **越权/隔离突破**：团队进度共享必须以脱敏摘要为边界，严禁直接读取他人原始选品/店铺数据；需统一鉴权中间件 + `team_id` 行级隔离，作为安全底线。
- **与个体隔离冲突**：明确"默认私有、显式同意才共享"原则，避免用户误以为加入团队即公开全部数据。
- **进度口径不一致**：成员"战略/类目/决策"口径须与 F4/F6/F10 对齐，避免看板与个体结果对不上。

---

## 附录：评分卡维度初稿

| 维度 | 含义 | 数据来源 |
|------|------|----------|
| 匹配度 | 与用户自然语言意图契合度 | 解析条件命中 |
| 变现力 | 销售额规模（价×量，非真实毛利） | 产品元数据(price) + 周销量(units_sold) |
| 趋势性 | 近期销量增长势头（基于具体销量件数） | 周销量件数（rank 仅作类目内展示） |
| 竞争度 | 类目红海/蓝海（销量集中度，越低越优） | 类目内销量集中度 + 同参与者选品（MVP 不含） |
| 受众契合 | 与目标人群（反推）匹配 | 受众反推 |

> 默认权重：匹配度 30% · 趋势性 25% · 变现力 20% · 竞争度 15% · 受众契合 10%，用户可在确认环节微调。
